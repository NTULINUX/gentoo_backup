CC="gcc"
CXX="g++"
LD="ld.bfd"
AR="ar"
AS="as"
OBJCOPY="objcopy"
OBJDUMP="objdump"
NM="nm"
RANLIB="ranlib"
READELF="readelf"
STRIP="strip"

LDFLAGS="${LDFLAGS} -fuse-ld=bfd ${COMMON_FLAGS} ${TRIM_SIZE_LDFLAGS} -Wl,-z,lazy -Wl,--strip-all"
